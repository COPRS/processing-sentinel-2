apiVersion: v1
kind: Secret
type: Opaque
data:
  mongo.password: "{{ mongodb.password }}"
  mongo.username: "{{ mongodb.username }}"
  obs.accessKey: "{{ obs.accessKey }}"
  obs.secretKey: "{{ obs.secretKey }}"
metadata:
  name: pw-l0c
  namespace: processing

---

apiVersion: v1
kind: Secret
type: Opaque
data:
  obs.accessKey: "{{ obs.accessKey }}"
  obs.secretKey: "{{ obs.secretKey }}"
metadata:
  name: ew-l0c
  namespace: processing

